# Test for model directives convertion script
# This should fail as "directives" is not found first

something_not_directives_first:

directives:

# Top level model, for example the robot arm.
- add_model:
    name: top_level_model
    file: package://process_model_directives_test/simple_model.sdf

# -*- python -*-
load("//tools/lint:lint.bzl", "add_lint_tests")
load(
    "//tools/skylark:drake_cc.bzl",
    "drake_cc_binary",
)

package(
    default_visibility = ["//visibility:private"],
)

drake_cc_binary(
    name = "petsc_benchmark",
    srcs = ["petsc_benchmark.cc"],
    deps = [
        "@eigen",
        "@googlebenchmark//:benchmark",
        "@petsc",
    ],
)

add_lint_tests()

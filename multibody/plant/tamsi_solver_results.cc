#include "drake/multibody/plant/tamsi_solver_results.h"

// TODO(jamiesnape): The library of which this file is part causes a warning
// from libtool on macOS that the table of contents is empty (no object file
// members in the library define global symbols).

DRAKE_DEFINE_CLASS_TEMPLATE_INSTANTIATIONS_ON_DEFAULT_SCALARS(
    struct ::drake::multibody::internal::TamsiSolverResults)

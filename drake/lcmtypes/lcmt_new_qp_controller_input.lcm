package drake;

struct lcmt_new_qp_controller_input {
  // Desireds
  // TODO: add zmp stuff (lcmt_zmp_data zmp_data;)
  double comdd[3];
  double weight_comdd[3];

  int32_t num_vd;
  double vd[num_vd];
  double weight_vd[num_vd];

  int32_t num_bodydd;
  lcmt_body_motion bodydd[num_bodydd];

  // Contact information
  int32_t num_contact_info;
  lcmt_contact_information contact_info[num_contact_info];

  // TODO: External wrench

  // Dynamics residual
  double dynamics_residual[num_vd];
}

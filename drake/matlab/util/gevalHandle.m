function fcn=geval_handle(fun,varargin)

% For a method that is going to make repeated calls to geval for a 
% particular function, this method will do the case checking once 
% and handle back an efficient function handle implementation of 
% whichever gradient method would have been used by geval.

error('Coming soon');
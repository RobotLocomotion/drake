% Help file for the wrappers which provide a common interface to the
% multitude of nonlinear optimization packages that we might use.  
%
%  Calling
%    [x,fval] = *****_wrapper(x0,objfun,con,objgrad,congrad,objgrad_sparse,congrad_sparse,options)
%  
%
%  See also:  fmincon_wrapper, snopt_wrapper, ...
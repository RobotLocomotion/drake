set(DRAKE_PYTHON_DIR "${CMAKE_INSTALL_PREFIX}/lib/python2.7/site-packages")

file(RELATIVE_PATH RELATIVE_PATH_PYDRAKE_DRAKE
  "${DRAKE_PYTHON_DIR}/pydrake" "${PROJECT_SOURCE_DIR}")

configure_file(autogenerated_path.py.in autogenerated_path.py @ONLY)

install(DIRECTORY pydrake DESTINATION "${DRAKE_PYTHON_DIR}")
install(FILES "${CMAKE_CURRENT_BINARY_DIR}/autogenerated_path.py"
  DESTINATION "${DRAKE_PYTHON_DIR}/pydrake")

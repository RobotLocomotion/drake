
# NO LONGER ALLOWED, because it lead to strange memory errors (presumably because 
# the #ifdefs were triggering differently when called from different compilers)

# add_library(tinyxml tinyxml.cpp tinyxmlparser.cpp tinyxmlerror.cpp)


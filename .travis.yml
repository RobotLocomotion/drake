sudo: required
dist: precise

language: cpp

os:
  - osx

compiler:
  - clang

branches:
  only:
    - master

install:
  - source travis_install.sh

script:
  - source travis_script.sh

notifications:
  email: false

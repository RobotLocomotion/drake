# Core compilers.
gcc
gcc-c++
gcc-gfortran
nasm

# Clang (for mkdoc).
clang15-devel

# Other code tools.
cmake
make
pkgconf-pkg-config

# Other general tools.
diffutils
file
patch
patchelf
wget
which

# Build dependencies (general).
glib2-devel
libXt-devel

# Build dependencies (Python).
openssl-devel
xz
sqlite-devel

# Core compilers.
# When changing this, see wheel/image/build-drake.sh.
gcc-toolset-14-gcc
gcc-toolset-14-gcc-c++
gcc-toolset-14-gcc-gfortran
nasm

# Clang (for mkdoc).
clang15-devel

# Other code tools.
cmake
make
pkgconf-pkg-config

# Other general tools.
diffutils
file
patch
patchelf
wget
which

# Build dependencies (general).
glib2-devel
libXt-devel

# Build dependencies (Python).
openssl-devel
xz
sqlite-devel

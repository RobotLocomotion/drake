load("//tools/install:install.bzl", "install", "install_files")
load("//tools/lint:lint.bzl", "add_lint_tests")

install_files(
    name = "install_license",
    dest = "share/doc/robin_map",
    files = ["@robin-map//:LICENSE"],
    allowed_externals = ["@robin-map//:LICENSE"],
)

install(
    name = "install",
    visibility = ["@nanobind//:__pkg__"],
    deps = [":install_license"],
)

add_lint_tests()

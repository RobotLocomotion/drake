load("//tools/lint:lint.bzl", "add_lint_tests")
load("//tools/skylark:drake_cc.bzl", "drake_cc_googletest")

drake_cc_googletest(
    name = "is_zero_uses_relative_epsilon_test",
    deps = [
        "@fcl_internal//:fcl",
    ],
)

add_lint_tests()

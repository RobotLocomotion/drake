[pybind11] Add synonyms that are forward-compatible with nanobind

These help ease Drake's transition to nanobind.
See https://github.com/RobotLocomotion/drake/issues/21572.

--- include/pybind11/attr.h
+++ include/pybind11/attr.h
@@ -118,6 +118,9 @@
 /// Annotation to mark enums as an arithmetic type
 struct arithmetic {};
 
+// Drake-specific forward-compatible synonym for nanobind.
+using is_arithmetic = arithmetic;
+
 /// Mark a function for addition at the beginning of the existing overload chain instead of the end
 struct prepend {};
 

--- include/pybind11/detail/common.h
+++ include/pybind11/detail/common.h
@@ -566,6 +566,9 @@
     reference_internal
 };
 
+// Drake-specific forward-compatible synonym for nanobind.
+using rv_policy = return_value_policy;
+
 PYBIND11_NAMESPACE_BEGIN(detail)
 
 inline static constexpr int log2(size_t n, int k = 0) {

--- include/pybind11/pybind11.h
+++ include/pybind11/pybind11.h
@@ -1281,6 +1281,9 @@
         }
         return reinterpret_steal<module_>(obj);
     }
+    static module_ import_(const char *name) {
+        return module_::import(name);
+    }
 
     /// Reload the module or throws `error_already_set`.
     void reload() {
@@ -2106,6 +2106,7 @@
 
 public:
     using type = type_;
+    using Type = type_;
     using type_alias = detail::exactly_one_t<is_subtype, void, options...>;
     constexpr static bool has_alias = !std::is_void<type_alias>::value;
     using holder_type = detail::exactly_one_t<is_holder, default_holder_type<type>, options...>;
@@ -1901,6 +1901,40 @@
         return *this;
     }
 
+    // Drake-specific forward-compatible synonyms for nanobind.
+    template <typename... Args>
+    auto def_ro(Args&&... args) -> decltype(def_readonly(std::forward<Args>(args)...)) {
+        return def_readonly(std::forward<Args>(args)...);
+    }
+    template <typename... Args>
+    auto def_ro_static(Args&&... args) -> decltype(def_readonly_static(std::forward<Args>(args)...)) {
+        return def_readonly_static(std::forward<Args>(args)...);
+    }
+    template <typename... Args>
+    auto def_rw(Args&&... args) -> decltype(def_readwrite(std::forward<Args>(args)...)) {
+        return def_readwrite(std::forward<Args>(args)...);
+    }
+    template <typename... Args>
+    auto def_rw_static(Args&&... args) -> decltype(def_readwrite_static(std::forward<Args>(args)...)) {
+        return def_readwrite_static(std::forward<Args>(args)...);
+    }
+    template <typename... Args>
+    auto def_prop_ro(Args&&... args) -> decltype(def_property_readonly(std::forward<Args>(args)...)) {
+        return def_property_readonly(std::forward<Args>(args)...);
+    }
+    template <typename... Args>
+    auto def_prop_ro_static(Args&&... args) -> decltype(def_property_readonly_static(std::forward<Args>(args)...)) {
+        return def_property_readonly_static(std::forward<Args>(args)...);
+    }
+    template <typename... Args>
+    auto def_prop_rw(Args&&... args) -> decltype(def_property(std::forward<Args>(args)...)) {
+        return def_property(std::forward<Args>(args)...);
+    }
+    template <typename... Args>
+    auto def_prop_rw_static(Args&&... args) -> decltype(def_property_static(std::forward<Args>(args)...)) {
+        return def_property_static(std::forward<Args>(args)...);
+    }
+
 private:
     /// Initialize holder object, variant 1: object derives from enable_shared_from_this
     template <typename T>

--- include/pybind11/pytypes.h
+++ include/pybind11/pytypes.h
@@ -196,6 +196,7 @@
     // TODO PYBIND11_DEPRECATED(
     //     "Call py::type::handle_of(h) or py::type::of(h) instead of h.get_type()")
     handle get_type() const;
+    handle type() const;
 
 private:
     bool rich_compare(object_api const &other, int value) const;
@@ -485,6 +485,7 @@
 T reinterpret_borrow(handle h) {
     return {h, object::borrowed_t{}};
 }
+template <typename T> T borrow(handle h) { return reinterpret_borrow<T>(h); }
 
 /** \rst
     Like `reinterpret_borrow`, but steals the reference.
@@ -1630,6 +1630,8 @@
         return std::string(buffer, (size_t) length);
     }
 
+    const char *c_str() const { return PyUnicode_AsUTF8AndSize(m_ptr, nullptr); }
+
     template <typename... Args>
     str format(Args &&...args) const {
         return attr("format")(std::forward<Args>(args)...);
@@ -1743,6 +1743,9 @@ public:
     // NOLINTNEXTLINE(google-explicit-constructor)
     operator std::string_view() const { return string_op<std::string_view>(); }
 #endif
+
+    const char *c_str() const { return PyBytes_AsString(m_ptr); }
+    size_t size() const { return (size_t) PyBytes_Size(m_ptr); }
 private:
     template <typename T>
     T string_op() const {
@@ -2272,4 +2272,6 @@
 };
 
+using callable = function;
+
 class staticmethod : public object {
 public:
@@ -2549,6 +2549,11 @@
 }
 
 template <typename D>
+handle object_api<D>::type() const {
+    return type::handle_of(derived());
+}
+
+template <typename D>
 bool object_api<D>::rich_compare(object_api const &other, int value) const {
     int rv = PyObject_RichCompareBool(derived().ptr(), other.derived().ptr(), value);
     if (rv == -1) {

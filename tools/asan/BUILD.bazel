load("//tools/lint:lint.bzl", "add_lint_tests")
load("//tools/skylark:sh.bzl", "sh_binary")

package(default_visibility = ["//visibility:private"])

sh_binary(
    name = "asan",
    srcs = ["asan.sh"],
    data = [
        "asan.supp",
        "//tools/lsan:lsan.supp",
    ],
)

add_lint_tests()

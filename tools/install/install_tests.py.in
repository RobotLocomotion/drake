#!/usr/bin/env python

# This file is configured automatically.

import os
import subprocess
import unittest
import install_test_helper


class TestCommonInstall(unittest.TestCase):
    def testDrakeInstall(self):
        # Install into bazel temporary directory. The temporary directory does
        # not need to be removed as bazel tests are run in a scratch space.
        install_test_helper.install()
        # Execute the install actions.
        <<actions>>


if __name__ == '__main__':
    unittest.main()

#!/usr/bin/env python3
"""
Installation script generated from a Bazel `install` target.
"""

# N.B. Please ensure any significant behavior is tested by `install_meta_test`.
# Example execution:
#   $ bazel test //tools/install:py/install_meta_test --test_output=streamed --nocache_test_results  # noqa

# N.B. This is designed to emulate CMake's install mechanism. Do not add
# unnecessary print statements.

import os
import sys

# TODO(rpoyner-tri) This path surgery is regrettable, but probably requires
# more bazel cleverness to completely remove.
sys.path.append(os.path.join(os.getcwd(), '..'))

# This import list should only import `execute_installation` and the small
# number of functions that are allowed in the template expansion of actions.
from drake.tools.install.install_lib import (
    create_java_launcher,
    execute_installation,
    install,
)


def install_actions_function():
    # Execute the install actions.
    <<actions>>


if __name__ == "__main__":
    execute_installation(sys.argv[1:], install_actions_function)

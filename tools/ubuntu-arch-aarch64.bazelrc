# Options for Ubuntu when running on arm64 as reported by `arch`.

# We don't support Gurobi on arm64.
build:everything --@drake//tools/flags:with_gurobi=False
build:everything --test_tag_filters=-no_everything,-gurobi

package drake;

struct lcmt_body_motion_data
{
	int64_t timestamp;

	int32_t body_id;

	double ts[2];
	double coefs[6][4];
	boolean in_floating_base_nullspace;
	boolean control_pose_when_in_contact;

        boolean use_spatial_velocity;
        double quat_task_to_world[4];
        double translation_task_to_world[4];
        double xyz_kp_multiplier[3];
        double xyz_kd_multiplier[3];
        double expmap_kp_multiplier;
        double expmap_kd_multiplier;
}

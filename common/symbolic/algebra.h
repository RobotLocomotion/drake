#pragma once

/// @file
/// When using Drake's symbolic algebra types (drake::symbolic::Expression or
/// drake::symbolic::Formula), use `#include <drake/common/symbolic/algebra.h>`
/// as your include statement.

// Delegate the internal implementation details to our subdirectory.
#include "drake/common/symbolic/algebra/all.h"
